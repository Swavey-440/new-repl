const display = require('./example.js');
const os = require('os');
const fs = require('fs');
const path = require('path');

console.log(os.uptime());
console.log(os.freemem());
console.log(os.totalmem());

// console.log(fs.writeFile());
console.log(path.sep);
 







